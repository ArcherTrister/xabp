/* This file is automatically generated by ABP framework to use MVC Controllers from javascript. */


// module payment

(function(){

  // controller x.abp.payment.gateways.gateway

  (function(){

    abp.utils.createNamespace(window, 'x.abp.payment.gateways.gateway');

    x.abp.payment.gateways.gateway.getGatewayConfiguration = function(ajaxParams) {
      return abp.ajax($.extend(true, {
        url: abp.appPath + 'api/payment/gateways',
        type: 'GET'
      }, ajaxParams));
    };

    x.abp.payment.gateways.gateway.getSubscriptionSupportedGateways = function(ajaxParams) {
      return abp.ajax($.extend(true, {
        url: abp.appPath + 'api/payment/gateways/subscription-supported',
        type: 'GET'
      }, ajaxParams));
    };

  })();

  // controller x.abp.payment.plans.plan

  (function(){

    abp.utils.createNamespace(window, 'x.abp.payment.plans.plan');

    x.abp.payment.plans.plan.getGatewayPlan = function(planId, gateway, ajaxParams) {
      return abp.ajax($.extend(true, {
        url: abp.appPath + 'api/payment/plans/' + planId + '/' + gateway + '',
        type: 'GET'
      }, ajaxParams));
    };

    x.abp.payment.plans.plan.getPlanList = function(ajaxParams) {
      return abp.ajax($.extend(true, {
        url: abp.appPath + 'api/payment/plans',
        type: 'GET'
      }, ajaxParams));
    };

    x.abp.payment.plans.plan.get = function(planId, ajaxParams) {
      return abp.ajax($.extend(true, {
        url: abp.appPath + 'api/payment/plans/' + planId + '',
        type: 'GET'
      }, ajaxParams));
    };

    x.abp.payment.plans.plan.getMany = function(planIds, ajaxParams) {
      return abp.ajax($.extend(true, {
        url: abp.appPath + 'api/payment/plans/many' + abp.utils.buildQueryString([{ name: 'planIds', value: planIds }]) + '',
        type: 'GET'
      }, ajaxParams));
    };

  })();

  // controller x.abp.payment.requests.paymentRequest

  (function(){

    abp.utils.createNamespace(window, 'x.abp.payment.requests.paymentRequest');

    x.abp.payment.requests.paymentRequest.complete = function(paymentGateway, parameters, paymentMethod, ajaxParams) {
      return abp.ajax($.extend(true, {
        url: abp.appPath + 'api/payment/' + paymentMethod + '/complete' + abp.utils.buildQueryString([{ name: 'paymentGateway', value: paymentGateway }]) + '',
        type: 'POST',
        data: JSON.stringify(parameters)
      }, ajaxParams));
    };

    x.abp.payment.requests.paymentRequest.create = function(input, ajaxParams) {
      return abp.ajax($.extend(true, {
        url: abp.appPath + 'api/payment/requests',
        type: 'POST',
        data: JSON.stringify(input)
      }, ajaxParams));
    };

    x.abp.payment.requests.paymentRequest.get = function(id, ajaxParams) {
      return abp.ajax($.extend(true, {
        url: abp.appPath + 'api/payment/requests/' + id + '',
        type: 'GET'
      }, ajaxParams));
    };

    x.abp.payment.requests.paymentRequest.handleWebhook = function(paymentGateway, payload, headers, paymentMethod, ajaxParams) {
      return abp.ajax($.extend(true, {
        url: abp.appPath + 'api/payment/' + paymentMethod + '/webhook' + abp.utils.buildQueryString([{ name: 'paymentGateway', value: paymentGateway }, { name: 'payload', value: payload }]) + '',
        type: 'POST',
        headers: {
          'headers': headers
        }
      }, ajaxParams));
    };

    x.abp.payment.requests.paymentRequest.start = function(paymentGateway, input, paymentMethod, ajaxParams) {
      return abp.ajax($.extend(true, {
        url: abp.appPath + 'api/payment/' + paymentMethod + '/start' + abp.utils.buildQueryString([{ name: 'paymentGateway', value: paymentGateway }]) + '',
        type: 'POST',
        data: JSON.stringify(input)
      }, ajaxParams));
    };

  })();

})();


