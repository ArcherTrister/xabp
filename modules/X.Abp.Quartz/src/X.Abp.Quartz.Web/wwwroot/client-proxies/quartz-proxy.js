/* This file is automatically generated by ABP framework to use MVC Controllers from javascript. */


// module quartz

(function(){

  // controller x.abp.quartz.calendar

  (function(){

    abp.utils.createNamespace(window, 'x.abp.quartz.calendar');

    x.abp.quartz.calendar.getList = function(schedulerName, ajaxParams) {
      return abp.ajax($.extend(true, {
        url: abp.appPath + 'api/quartz/' + schedulerName + '/calendars',
        type: 'GET'
      }, ajaxParams));
    };

    x.abp.quartz.calendar.get = function(schedulerName, calendarName, ajaxParams) {
      return abp.ajax($.extend(true, {
        url: abp.appPath + 'api/quartz/' + schedulerName + '/calendars/' + calendarName + '',
        type: 'GET'
      }, ajaxParams));
    };

    x.abp.quartz.calendar.create = function(schedulerName, calendarName, replace, updateTriggers, ajaxParams) {
      return abp.ajax($.extend(true, {
        url: abp.appPath + 'api/quartz/' + schedulerName + '/calendars/' + calendarName + '' + abp.utils.buildQueryString([{ name: 'replace', value: replace }, { name: 'updateTriggers', value: updateTriggers }]) + '',
        type: 'PUT',
        dataType: null
      }, ajaxParams));
    };

    x.abp.quartz.calendar['delete'] = function(schedulerName, calendarName, ajaxParams) {
      return abp.ajax($.extend(true, {
        url: abp.appPath + 'api/quartz/' + schedulerName + '/calendars/' + calendarName + '',
        type: 'DELETE',
        dataType: null
      }, ajaxParams));
    };

  })();

  // controller x.abp.quartz.executionHistory

  (function(){

    abp.utils.createNamespace(window, 'x.abp.quartz.executionHistory');

    x.abp.quartz.executionHistory.getList = function(input, ajaxParams) {
      return abp.ajax($.extend(true, {
        url: abp.appPath + 'api/quartz/execution-histories' + abp.utils.buildQueryString([{ name: 'schedulerName', value: input.schedulerName }, { name: 'sorting', value: input.sorting }, { name: 'skipCount', value: input.skipCount }, { name: 'maxResultCount', value: input.maxResultCount }]) + '',
        type: 'GET'
      }, ajaxParams));
    };

  })();

  // controller x.abp.quartz.job

  (function(){

    abp.utils.createNamespace(window, 'x.abp.quartz.job');

    x.abp.quartz.job.getList = function(schedulerName, input, ajaxParams) {
      return abp.ajax($.extend(true, {
        url: abp.appPath + 'api/quartz/' + schedulerName + '/jobs' + abp.utils.buildQueryString([{ name: 'groupMatcher.NameContains', value: input.groupMatcher.nameContains }, { name: 'groupMatcher.NameEndsWith', value: input.groupMatcher.nameEndsWith }, { name: 'groupMatcher.NameStartsWith', value: input.groupMatcher.nameStartsWith }, { name: 'groupMatcher.NameEquals', value: input.groupMatcher.nameEquals }, { name: 'sorting', value: input.sorting }, { name: 'skipCount', value: input.skipCount }, { name: 'maxResultCount', value: input.maxResultCount }]) + '',
        type: 'GET'
      }, ajaxParams));
    };

    x.abp.quartz.job.get = function(schedulerName, jobGroup, jobName, ajaxParams) {
      return abp.ajax($.extend(true, {
        url: abp.appPath + 'api/quartz/' + schedulerName + '/jobs/' + jobGroup + '/' + jobName + '/details',
        type: 'GET'
      }, ajaxParams));
    };

    x.abp.quartz.job.currentlyExecutings = function(schedulerName, input, ajaxParams) {
      return abp.ajax($.extend(true, {
        url: abp.appPath + 'api/quartz/' + schedulerName + '/jobs/currently-executings' + abp.utils.buildQueryString([{ name: 'groupMatcher.NameContains', value: input.groupMatcher.nameContains }, { name: 'groupMatcher.NameEndsWith', value: input.groupMatcher.nameEndsWith }, { name: 'groupMatcher.NameStartsWith', value: input.groupMatcher.nameStartsWith }, { name: 'groupMatcher.NameEquals', value: input.groupMatcher.nameEquals }, { name: 'sorting', value: input.sorting }, { name: 'skipCount', value: input.skipCount }, { name: 'maxResultCount', value: input.maxResultCount }]) + '',
        type: 'GET'
      }, ajaxParams));
    };

    x.abp.quartz.job.pause = function(schedulerName, jobGroup, jobName, ajaxParams) {
      return abp.ajax($.extend(true, {
        url: abp.appPath + 'api/quartz/' + schedulerName + '/jobs/' + jobGroup + '/' + jobName + '/pause',
        type: 'POST',
        dataType: null
      }, ajaxParams));
    };

    x.abp.quartz.job.batchPause = function(schedulerName, groupMatcher, ajaxParams) {
      return abp.ajax($.extend(true, {
        url: abp.appPath + 'api/quartz/' + schedulerName + '/jobs/pause',
        type: 'POST',
        dataType: null,
        data: JSON.stringify(groupMatcher)
      }, ajaxParams));
    };

    x.abp.quartz.job.resume = function(schedulerName, jobGroup, jobName, ajaxParams) {
      return abp.ajax($.extend(true, {
        url: abp.appPath + 'api/quartz/' + schedulerName + '/jobs/' + jobGroup + '/' + jobName + '/resume',
        type: 'POST',
        dataType: null
      }, ajaxParams));
    };

    x.abp.quartz.job.batchResume = function(schedulerName, groupMatcher, ajaxParams) {
      return abp.ajax($.extend(true, {
        url: abp.appPath + 'api/quartz/' + schedulerName + '/jobs/resume',
        type: 'POST',
        dataType: null,
        data: JSON.stringify(groupMatcher)
      }, ajaxParams));
    };

    x.abp.quartz.job.trigger = function(schedulerName, jobGroup, jobName, ajaxParams) {
      return abp.ajax($.extend(true, {
        url: abp.appPath + 'api/quartz/' + schedulerName + '/jobs/' + jobGroup + '/' + jobName + '/trigger',
        type: 'POST',
        dataType: null
      }, ajaxParams));
    };

    x.abp.quartz.job['delete'] = function(schedulerName, jobGroup, jobName, ajaxParams) {
      return abp.ajax($.extend(true, {
        url: abp.appPath + 'api/quartz/' + schedulerName + '/jobs/' + jobGroup + '/' + jobName + '',
        type: 'DELETE',
        dataType: null
      }, ajaxParams));
    };

    x.abp.quartz.job.interrupt = function(schedulerName, jobGroup, jobName, ajaxParams) {
      return abp.ajax($.extend(true, {
        url: abp.appPath + 'api/quartz/' + schedulerName + '/jobs/' + jobGroup + '/' + jobName + '/interrupt',
        type: 'POST',
        dataType: null
      }, ajaxParams));
    };

    x.abp.quartz.job.create = function(schedulerName, input, ajaxParams) {
      return abp.ajax($.extend(true, {
        url: abp.appPath + 'api/quartz/' + schedulerName + '/jobs',
        type: 'POST',
        dataType: null,
        data: JSON.stringify(input)
      }, ajaxParams));
    };

  })();

  // controller x.abp.quartz.scheduler

  (function(){

    abp.utils.createNamespace(window, 'x.abp.quartz.scheduler');

    x.abp.quartz.scheduler.getList = function(input, ajaxParams) {
      return abp.ajax($.extend(true, {
        url: abp.appPath + 'api/quartz/schedulers' + abp.utils.buildQueryString([{ name: 'sorting', value: input.sorting }, { name: 'skipCount', value: input.skipCount }, { name: 'maxResultCount', value: input.maxResultCount }]) + '',
        type: 'GET'
      }, ajaxParams));
    };

    x.abp.quartz.scheduler.get = function(schedulerName, ajaxParams) {
      return abp.ajax($.extend(true, {
        url: abp.appPath + 'api/quartz/schedulers/' + schedulerName + '',
        type: 'GET'
      }, ajaxParams));
    };

    x.abp.quartz.scheduler.start = function(schedulerName, delayMilliseconds, ajaxParams) {
      return abp.ajax($.extend(true, {
        url: abp.appPath + 'api/quartz/schedulers/' + schedulerName + '/start' + abp.utils.buildQueryString([{ name: 'delayMilliseconds', value: delayMilliseconds }]) + '',
        type: 'POST',
        dataType: null
      }, ajaxParams));
    };

    x.abp.quartz.scheduler.standby = function(schedulerName, ajaxParams) {
      return abp.ajax($.extend(true, {
        url: abp.appPath + 'api/quartz/schedulers/' + schedulerName + '/standby',
        type: 'POST',
        dataType: null
      }, ajaxParams));
    };

    x.abp.quartz.scheduler.shutdown = function(schedulerName, waitForJobsToComplete, ajaxParams) {
      return abp.ajax($.extend(true, {
        url: abp.appPath + 'api/quartz/schedulers/' + schedulerName + '/shutdown' + abp.utils.buildQueryString([{ name: 'waitForJobsToComplete', value: waitForJobsToComplete }]) + '',
        type: 'POST',
        dataType: null
      }, ajaxParams));
    };

    x.abp.quartz.scheduler.clear = function(schedulerName, ajaxParams) {
      return abp.ajax($.extend(true, {
        url: abp.appPath + 'api/quartz/schedulers/' + schedulerName + '/clear',
        type: 'POST',
        dataType: null
      }, ajaxParams));
    };

  })();

  // controller x.abp.quartz.trigger

  (function(){

    abp.utils.createNamespace(window, 'x.abp.quartz.trigger');

    x.abp.quartz.trigger.getList = function(schedulerName, groupMatcher, ajaxParams) {
      return abp.ajax($.extend(true, {
        url: abp.appPath + 'api/quartz/' + schedulerName + '/triggers' + abp.utils.buildQueryString([{ name: 'nameContains', value: groupMatcher.nameContains }, { name: 'nameEndsWith', value: groupMatcher.nameEndsWith }, { name: 'nameStartsWith', value: groupMatcher.nameStartsWith }, { name: 'nameEquals', value: groupMatcher.nameEquals }]) + '',
        type: 'GET'
      }, ajaxParams));
    };

    x.abp.quartz.trigger.get = function(schedulerName, triggerGroup, triggerName, ajaxParams) {
      return abp.ajax($.extend(true, {
        url: abp.appPath + 'api/quartz/' + schedulerName + '/triggers/' + triggerGroup + '/' + triggerName + '/details',
        type: 'GET'
      }, ajaxParams));
    };

    x.abp.quartz.trigger.pause = function(schedulerName, triggerGroup, triggerName, ajaxParams) {
      return abp.ajax($.extend(true, {
        url: abp.appPath + 'api/quartz/' + schedulerName + '/triggers/' + triggerGroup + '/' + triggerName + '/pause',
        type: 'POST',
        dataType: null
      }, ajaxParams));
    };

    x.abp.quartz.trigger.batchPause = function(schedulerName, groupMatcher, ajaxParams) {
      return abp.ajax($.extend(true, {
        url: abp.appPath + 'api/quartz/' + schedulerName + '/triggers/pause',
        type: 'POST',
        dataType: null,
        data: JSON.stringify(groupMatcher)
      }, ajaxParams));
    };

    x.abp.quartz.trigger.resume = function(schedulerName, triggerGroup, triggerName, ajaxParams) {
      return abp.ajax($.extend(true, {
        url: abp.appPath + 'api/quartz/' + schedulerName + '/triggers/' + triggerGroup + '/' + triggerName + '/resume',
        type: 'POST',
        dataType: null
      }, ajaxParams));
    };

    x.abp.quartz.trigger.batchResume = function(schedulerName, groupMatcher, ajaxParams) {
      return abp.ajax($.extend(true, {
        url: abp.appPath + 'api/quartz/' + schedulerName + '/triggers/resume',
        type: 'POST',
        dataType: null,
        data: JSON.stringify(groupMatcher)
      }, ajaxParams));
    };

  })();

})();


