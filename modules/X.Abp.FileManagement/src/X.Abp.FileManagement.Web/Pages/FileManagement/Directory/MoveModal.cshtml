@page
@using Microsoft.AspNetCore.Mvc.Localization;
@using Volo.Abp.AspNetCore.Mvc.UI.Bootstrap.TagHelpers.Modal;
@using X.Abp.FileManagement.Localization;
@model X.Abp.FileManagement.Web.Pages.FileManagement.Directory.MoveModalModel
@inject IHtmlLocalizer<FileManagementResource> L

@{
    Layout = null;
}

<form data-ajaxForm="true" asp-page="/FileManagement/Directory/MoveModal" asp-route-id="@Model.Id" autocomplete="off">
    <abp-modal>
        <abp-modal-header title="@L["Move"].Value"></abp-modal-header>

        <abp-modal-body>
            <abp-row>
                <abp-column>
                    <span class="p-2 d-block">@L["MoveToUnder"] </span>
                    <ol class="breadcrumb" id="DirectoryPathList"></ol>
                </abp-column>
            </abp-row>

            <abp-row class="mt-2">
                <abp-column>

                    <abp-input type="hidden" asp-for="@Model.MoveDirectoryInput.Id" />

                    <abp-input type="hidden" asp-for="@Model.MoveDirectoryInput.NewParentId" />
                    <abp-input type="hidden" asp-for="@Model.MoveDirectoryInput.ConcurrencyStamp" />

                    <abp-table id="MoveDirectory_DirectoryContentTable" directory-id="" striped-rows="true" class="nowrap"></abp-table>
                </abp-column>
            </abp-row>
        </abp-modal-body>

        <abp-modal-footer buttons="@(AbpModalButtons.Cancel|AbpModalButtons.Save)"></abp-modal-footer>
    </abp-modal>
</form>
